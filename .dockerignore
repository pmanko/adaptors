# Node.js dependencies (will be rebuilt in container)
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.pnpm-store/

# Build outputs that will be generated in container
dist/
build/
**/dist/
**/build/
coverage/
.nyc_output/

# Logs
logs/
*.log

# Environment files (sensitive data)
.env
.env.*
!.env.example

# Git metadata
.git/
.gitignore
.gitattributes

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
tmp/
temp/
*.tmp
*.temp
*.bak
*.cache

# CI/CD files
.github/
.gitlab-ci.yml
.travis.yml
.circleci/

# Docker files (no need to copy these into the image)
Dockerfile*
docker-compose*.yml

# Large files that aren't needed for build
*.tgz
*.tar.gz
.npm/
.eslintcache
.stylelintcache

# Keep test files and README - we need these for our testing approach
# Keep package-lock.json and pnpm-lock.yaml - needed for proper dependency resolution 